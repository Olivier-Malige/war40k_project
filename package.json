{
  "name": "warhammer_project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "docker-compose rm -vf",
    "logs": "docker-compose logs -f --tail 10",
    "pull": "docker-compose pull",
    "reset": "npm run stop && npm run clean && docker-compose build && npm run start",
    "restart": "npm run stop && npm run start",
    "start": "docker-compose up -d",
    "stop": "docker-compose kill"
  }
}
